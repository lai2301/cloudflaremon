name: Validate Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Validate Configuration
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate services.json
        run: |
          echo "Validating services.json..."
          if ! jq empty services.json 2>/dev/null; then
            echo "❌ services.json is not valid JSON"
            exit 1
          fi
          echo "✅ services.json is valid"

      - name: Check KV namespace status
        run: |
          echo "Checking KV namespace status..."
          if grep -q "YOUR_KV_NAMESPACE_ID_HERE" wrangler.toml; then
            echo "⚠️  KV namespace not yet configured (placeholder found)"
            echo "✅ This is OK! The deployment workflow will create it automatically using Terraform."
            echo ""
            echo "📝 What will happen:"
            echo "  1. Merge this PR"
            echo "  2. Deployment workflow runs automatically"
            echo "  3. Terraform creates KV namespace"
            echo "  4. wrangler.toml gets updated and committed"
            echo "  5. Worker deploys successfully"
          else
            echo "✅ KV namespace is already configured"
          fi

      - name: Validate wrangler.toml syntax
        run: |
          echo "Validating wrangler.toml syntax..."
          # Just check the file is valid TOML, don't try to deploy
          if ! grep -q "^name = " wrangler.toml; then
            echo "❌ wrangler.toml appears invalid"
            exit 1
          fi
          if ! grep -q "^main = " wrangler.toml; then
            echo "❌ wrangler.toml missing 'main' field"
            exit 1
          fi
          echo "✅ wrangler.toml syntax is valid"

      - name: Check if first-time setup
        id: check_setup
        run: |
          if grep -q "YOUR_KV_NAMESPACE_ID_HERE" wrangler.toml; then
            echo "is_first_time=true" >> $GITHUB_OUTPUT
          else
            echo "is_first_time=false" >> $GITHUB_OUTPUT
          fi

      - name: Comment on PR (First-Time Setup)
        uses: actions/github-script@v7
        if: success() && steps.check_setup.outputs.is_first_time == 'true'
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const body = `✅ Configuration validated successfully!

            **🎉 First-Time Setup Detected**

            This appears to be your first deployment. Here's what will happen when you merge:

            1. ✨ The deployment workflow will automatically run
            2. 🏗️ Terraform will create the KV namespace
            3. 📝 \`wrangler.toml\` will be updated with the namespace ID
            4. 💾 Changes will be committed automatically
            5. 🚀 Your worker will deploy successfully

            **No manual steps required!** Just merge this PR and watch the automation work.

            📖 [View Deployment Documentation](https://github.com/${context.repo.owner}/${context.repo.repo}/blob/main/docs/DEPLOYMENT.md)`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            })

      - name: Comment on PR (Normal)
        uses: actions/github-script@v7
        if: success() && steps.check_setup.outputs.is_first_time == 'false'
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ Configuration validated successfully! Ready to merge and deploy.'
            })

