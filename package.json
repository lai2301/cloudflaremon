{
  "name": "cloudflare-heartbeat-monitor",
  "version": "1.0.0",
  "description": "Push-based Cloudflare Worker heartbeat monitor for internal services - no exposure required",
  "main": "src/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "monitoring",
    "heartbeat"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.15",
    "vitest": "^3.2.4",
    "wrangler": "^4.95.0"
  }
}
